{
  "name": "japanese-immersion-practice",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @jip/client build",
    "cloudflare:types": "pnpm --filter @jip/server types",
    "db:create": "pnpm --filter @jip/server db:create",
    "db:migrate:local": "pnpm --filter @jip/server db:migrate:local",
    "db:migrate:remote": "pnpm --filter @jip/server db:migrate:remote",
    "deploy": "pnpm --filter @jip/client deploy",
    "dev": "pnpm --filter @jip/client dev",
    "format": "oxfmt",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r typecheck",
    "lint:all": "NODE_OPTIONS='--experimental-strip-types' oxlint --disable-nested-config",
    "lint": "NODE_OPTIONS='--experimental-strip-types' oxlint --disable-nested-config --quiet",
    "lint:types": "NODE_OPTIONS='--experimental-strip-types' node packages/typed-lint/src/main.ts",
    "export:schemas": "pnpm --filter @jip/machines export:schemas",
    "verify": "pnpm run format && pnpm run typecheck && pnpm run lint:all && pnpm run lint:types"
  },
  "devDependencies": {
    "@effect/language-service": "^0.86.2",
    "@jip/oxc": "workspace:^",
    "@jip/typed-lint": "workspace:^",
    "@types/node": "latest",
    "@typescript/native-preview": "7.0.0-dev.20260629.1",
    "oxfmt": "^0.46.0",
    "oxlint": "1.61.0",
    "typescript": "7.0.1-rc"
  },
  "packageManager": "pnpm@11.7.0"
}
